# Default to LF in the repo, with CRLF translated on checkout per platform default.
* text=auto eol=lf

# Shell scripts MUST be LF on every platform, because a Windows checkout otherwise breaks bash.
*.sh text eol=lf
